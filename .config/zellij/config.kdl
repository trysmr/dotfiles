// zellij設定ファイル
// キーバインドはデフォルト（モード方式）を使用
// 画面下部にキーヒントが表示されるため、覚える必要なし

// テーマ設定
theme "tokyo-night"

// マウスサポート有効化
mouse_mode true

// 選択時に自動コピー
copy_on_select true

// クリップボードにコピー時にOSCエスケープシーケンスを使用
copy_command "pbcopy"

// スクロールバッファサイズ
scroll_buffer_size 50000

// ペインフレームを表示
pane_frames true

// セッション名を自動生成
session_name "main"

// UIモード設定
ui {
    pane_frames {
        rounded_corners true
    }
}

// デフォルトレイアウト
default_layout "compact"
